name: Build and Deploy Docker Container
on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: deploy
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Private Registry
        uses: docker/login-action@v2
        with:
          registry: git.stuffworks.net
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            git.stuffworks.net/nextjs-app:latest
            git.stuffworks.net/nextjs-app:${{ github.sha }}
          cache-from: type=registry,ref=git.stuffworks.net/nextjs-app:latest
          cache-to: type=inline

      - name: Deploy container
        run: |
          docker pull git.stuffworks.net/nextjs-app:latest
          docker stop nextjs-app || true
          docker rm nextjs-app || true
          docker run -d \
            --name nextjs-app \
            --restart unless-stopped \
            -p 3000:3000 \
            git.stuffworks.net/nextjs-app:latest
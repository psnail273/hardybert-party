// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Household {
  id          Int       @id @default(autoincrement())
  name        String    @unique
  invitees    Invitee[] 
}

model Invitee {
  id                          Int       @id @default(autoincrement())
  initialName                 String    @unique
  name                        String    @unique
  isAttending                 Boolean   @default(false)
  hasRSVPed                   Boolean   @default(false)
  updatedAt                   DateTime  @updatedAt
  dieteryRestriction          Boolean   @default(false)
  hasPlusOneOption            Boolean   @default(false)
  plusOneIsAttending          Boolean   @default(false)
  plusOneName                 String?   @unique
  plusOneDietaryRestrictions  Boolean?  @default(false)
  household                   Household @relation(fields: [householdId], references: [id])
  householdId                 Int
}

